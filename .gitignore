# Runtime data (SQLite DB, generated once installed)
data/

# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
dist/
build/

# JS client SDK (clients/js/)
node_modules/
clients/js/dist/
clients/js/*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Playwright / test artifacts
.playwright-mcp/

# Downloaded Tailwind standalone CLI binary (scripts/build-css.sh fetches this
# on demand — a build tool, not source; only its output, app.css, is committed)
.tailwind-bin/

# Personal machine automation (Windows Scheduled Task script) — not part of the
# shipped product, specific to one dev machine's login-time recovery setup.
scripts/ensure-docker-running.ps1
scripts/ensure-docker-running.log
